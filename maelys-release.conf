# maelys-pilot declares the least a released product can: the socle's
# three default targets, a reviewer at the gate, and nothing else. What a
# trial needs beyond this is added by the trial, on a branch.
[gate]
reviewer
