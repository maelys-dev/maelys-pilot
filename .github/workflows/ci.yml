name: ci

# Created by maelys-release v0.59.1 (0.59.1) and owned by this repository: add
# jobs next to check; adopt keeps the socle line of check current.
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: maelys-dev/maelys-release/.github/workflows/check-product.yml@300794858a2c74d90ba822225001f3f50a585b77 # untagged
    with:
      product: maelys-pilot
