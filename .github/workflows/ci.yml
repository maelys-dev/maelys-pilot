name: ci

# Created by maelys-release v0.59.1 (0.59.1) and owned by this repository: add
# jobs next to check; adopt keeps the socle line of check current.
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: maelys-dev/maelys-release/.github/workflows/check-product.yml@010cb1f0c36ac41c05accd2b90db46ca12d84a8c # untagged
    with:
      product: maelys-pilot
